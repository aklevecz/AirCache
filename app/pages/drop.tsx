// Create cache or use existing cache - maybe this is another screen
//
// Pick an NFT
// . need the contract address
// . need the tokenId
// Approve the tx of NFT to the cache contract

export default function Drop() {
  return <div>drop it</div>;
}
